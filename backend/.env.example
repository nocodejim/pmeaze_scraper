# Backend Environment Variables

# Database URL
# For SQLite, this path is relative to the WORKDIR in the Docker container (/app)
# or relative to where the application is run if not using Docker.
DATABASE_URL=sqlite:///./quickbuild_rag.db
# Example for PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:port/database

# RAG JSON Data Path
# This path is relative to the WORKDIR in the Docker container (/app)
# or relative to where the application is run if not using Docker.
# For Docker Compose setup with the provided volume mount (./scraper/output:/app/scraper/output),
# the path inside the container will be 'scraper/output/all_content.json' assuming WORKDIR is /app.
RAG_JSON_PATH=scraper/output/all_content.json

# CORS Origins
# Comma-separated list of allowed origins.
# Pydantic will parse this into a List[str].
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","http://127.0.0.1:5173","http://127.0.0.1:3000"]
